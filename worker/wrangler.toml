name = "clip-analyst-api"
main = "index.js"
compatibility_date = "2024-04-10"

# Rate limiting: 10 requests per minute per IP
# https://developers.cloudflare.com/workers/configuration/rate-limiting/
[ratelimit]
  requests_per_period = 10
  period = 60

# CORS: Only allow requests from this origin
[vars]
ALLOWED_ORIGIN = "https://ssedtasak.github.io"

[[headers]]
  for = "/*"
  [headers.values]
    Access-Control-Allow-Origin = "*" # Override per-request in Worker using ALLOWED_ORIGIN
    Access-Control-Allow-Methods = "POST, OPTIONS"
    Access-Control-Allow-Headers = "Content-Type"

# Environment variables (set via `wrangler secret put`)
# Required: GEMINI_API_KEY (for AI video analysis)
# Optional: COBALT_API_KEY (for self-hosted Cobalt, uses public instance if not set)
